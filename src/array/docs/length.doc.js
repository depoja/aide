import { length } from "../length";

/**
 * Returns the number of elements in a given array.
 *
 * @param {*[]} array - The target array.
 * @returns {number} - The number of elements.
 *
 * @example
 * import { length } from "@srcode/aide";
 *
 * length([1, 2, 5]); // 3
 */
length;
