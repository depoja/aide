import { reverse } from "../reverse";

/**
 * Reverses the order of the elements in a given array.
 *
 * @param {*[]} array - The target array.
 *
 * @returns {*} - A new array with the reversed order.
 *
 * @example
 * import { reverse } from "@srcode/aide";
 *
 * reverse([1, 2, 5]); // [5, 2, 1]
 */
reverse;
