import { range } from "../range";
/**
 * Returns an array whose elements range **from** and **to** the given numbers.
 *
 * @param {number=} start - The start of the range.
 * @param {number} end - The end of the range.
 *
 * @returns {number[]} - An array containing the numbers in the range.
 *
 * @example
 * import { range } from "@srcode/aide";
 *
 * range(3, 6); // [3, 4, 5]
 * range(3); // [0, 1, 2]; same as calling range(0, 3)
 */
range;
